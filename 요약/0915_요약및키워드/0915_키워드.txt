콜백함수 => 함수의 매개변수가 함수인 함수(펑션콜링, 랭그래프 주요 개념 중 하나)
: 전화 부재 중 보고 다시 대응하던 걸 보고 호칭 => 이벤트가 발생하면 동작하라

밀리 마이크로 나노 피코 펨토 아토 젭토 욕토 세컨드 (10의 -3승 -6승 ...)

안티 앨리어싱 : AA => 계단현상을 해소하는..
아스키 테이블(ord) 27 => esc

# 데이터 수집 인사이트 : [퀵드로우] => 사람들한테서 게임형태로 공짜로 데이터를 얻는 사이트...
||norm|| => 선형대수에서 길이를 의미하는, || 파이프연산자 2개

포화연산 => 255값을 넘도록 더해지면 더 이상 더하지 않게 만드는 연산 saturate

명암비 조정 => 밝은 건 더 밝게 어두운 건 더 어둡게 => 값을 곱해서 구현하면..

Numpy에서는 dtype 설정이 몹시 중요한데,
특히 이미지와 관계하여 np.unit8을 쓴다는 걸 이해해야한다.

import numpy as np
data = np.arange(10,20)
print(np.where(data > 15, 99,data)) => 10,11,12,13,99,99,99,99

네트워크 ~= 리눅스
리눅스 man nano => manual 보여줘, nano의. 에디터 일종이고 VIM, VI 있음
ls -al 숨긴 파일까지 보여줘

파일속성 사용자 사용자그룹 사이즈 만든일시 이름
r read w write x execute // 3자씩 유저/그룹/other 3박자로 끊어서 권한 체크가 가능함
sudo : superuser do
chmod : change modi~~ => chang mode : 777 => 2진수로 1,2,4 읽어야하며 r d x 순서로 권한을 부여하는 내용
mkdir 작동 ok! 파일 생성은 touch 파일이름 으로 가능!
rm 파일 삭제 rmdir 디렉토리 삭제
exit 나가기

윈도우즈에 가상머신 올려야(BIOS) WSL이 잘 돌아간다