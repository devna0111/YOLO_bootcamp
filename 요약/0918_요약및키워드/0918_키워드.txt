영상의 산술 연산 => 합성의 의미를 갖는다. 
import cv2
cv2.bitwise_논리연산(a, b) => 비트연산이 가능하다

합성곱 => 벡터연산 필터링
필터링 => 합성곱(벡터연산) 연산이 아닌?? 다른 연산의 결과
필터링을 하게 되면 연산 결과 가장자리가 비는 결과(벡터연산은 차원이 바뀌는 현상이 발생하여) 
-> 이를 채우기 위해 다양한 BORDER_연산 방식이 있음.

anchor point => 고정점

가우시안 => 정규분포 모양을 의미한다고 생각하면 마음이 편하다

디퓨전 모델??? => 노이즈를 추가해 학습 후 원형을 복구하는 방식으로 모델을 학습하는 방식

cv => ROI : Region Of Interest : 관심영역 // 비관심영역

쓰레스(임계치)홀드  => 128을 기준으로 0/255 이진화 할 때 나오는 표현, 임계치.
Thresh : T로 주로 활용

오츠(오추) 알고리즘 : 분산을 기준으로 활용했다