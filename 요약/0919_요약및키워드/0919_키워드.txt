색조(Hue), 채도(Saturation), 명도(Value)

이진영상 : 0과 255로 처리한 영상(0과 1 대신 0과 255)
오츄 알고리즘 : 적당한 임계치를 정하는 최적화 방법 => thrashhold(임계치 기준 이진화) 에서 활용

텐서플로우 : 딥러닝 라이브러리, 고차원 행렬(텐서)의 흐름을 통해 딥러닝 알고리즘을 구현한다
케라스 : 함수로 해서 텐서플로우를 조작하는 라이브러리

기하 => 점선면, 차원, 벡터...

어파인 => affine 변환 : 값은 유지(픽셀 값)하면서 위치를 변경하는 방식, 직사각형을 평행사변형으로 만드는 등

단위 행렬 -> Identity, E (산수 속 1과 동일하다)

대각행렬 : 대각선 만 값이 있음(단위행렬과 형태는 동일, 값이 1에서 다른 값으로) => 주대각요소 : diagonal 
정사각행렬 == 정방행렬
(a b)
(c d)
의 행렬식 ad-bc = 0 => 역행렬 X => 행렬곱을 통해 단위행렬을 구성할 수 없음

역행렬 =>
   1    
-------|(d -b)
(ad-bc) (-c a)

-보간 interpolation

동차좌표 => 차원을 1개 추가해서 본다

CV에서 딥러닝은 최적 필터를 찾아내는 과정