saturated 포화된

명암 등을 통해 이미지 속 객체를 구분이 잘 되도록 전처리 => cv의 기본
np.where(조건, true값, false값) 을 활용해서 명암비를 뽑아내는 방식

Bagging 방식 약자 => Bootstrap Aggregating

효율적인 명암비 조절 : 128을 기준으로 색을 선명하게 해서 사용하는 방식
스트레칭 => 정규화 하고 255 값을 곱해 색을 선명하게 만드는 방식
평활화 => cumsum으로 색감을 더 선명하게 만드는 방식 => 밝은 건 더 밝게 어두운 건 더 어둡게

빌드툴.. 메이븐, 그레이든 등....

#docker 명령어 d : detach p port v volume i interactive t terminal
docker run -d -p 8080:80 -v /d/jjh/index.html:/usr/share/nginx/html/index.html nginx

docker run [OPTIONS] IMAGE [COMMAND] [ARG...]